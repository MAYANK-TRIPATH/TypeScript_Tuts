// Enums allows us to define a set of named constants. 

enum SeatChoice {
    AISLE = "aisle",
    MIDDLE = 3,
    WINDOW = "Seat",
    
}

const hcSeat = SeatChoice.AISLE

// Sometimes JS code for TS file is so much. to get rid we use "const".

const enum SeatChoice1 {
    AISLE = "aisle",
    MIDDLE = 3,
    WINDOW = "Seat",
    
}

const hcSeat1 = SeatChoice.AISLE