// data types
let sales = 123_234_432;
let course = "TypeScript";
let is_published = true;

// any data type :- Try to avoid any data type coz typescript is for defining type(No point of using TS).
let level;

function render(document: any) {
    console.log(document);
}